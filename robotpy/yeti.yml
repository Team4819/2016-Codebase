# This section defines the individual modules and module sets to run on startup.
enabled:
    - drivetrain
   # - dynamics_module

# This is a list of relative directories to search for modules
module_dirs:
    - modules

# Defines module sets
module_sets:
    drivetrain:
      - advanced_drive
      - basic_drive
